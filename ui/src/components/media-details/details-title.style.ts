import styled from "styled-components";

const Font = styled.div`
  font-family: "Poppins";
`

export const Title = styled(Font)`
  font-weight: 400;
  font-size: 1.5rem;
  color: #fff;
  text-align: left;
  padding-left: 1.5rem;
  margin-top: 1rem;
`;